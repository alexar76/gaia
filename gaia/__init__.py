"""GAIA — physical-world oracle gateway.

Virtual (and eventually real) IoT devices published as paid, Ed25519-signed
AIMarket v2 capabilities, with a statistics-based plausibility verifier that
speaks the Metis ``/v1/verify`` envelope — so the hub's Pay-on-Verified escrow
can settle sensor readings the same way it settles any other capability.
"""

__version__ = "0.1.0"
